# Agent Context for CPU Tests

Contains tests that execute kernels on the CPU backend using the IL code generator. The main entry is `RuntimeTests.cs`, which verifies memory operations and kernel execution.
